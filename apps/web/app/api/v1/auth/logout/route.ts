import { trust, authorizedContextForRoute, errorResponse } from '../../../../../lib/trust-app'; export async function POST(request: Request) { try { const context = await authorizedContextForRoute(request); await trust.identity.revokeSession(context.sessionId, context, 'LOGOUT'); const response = Response.json({ status: 'REVOKED' }); response.headers.append('Set-Cookie', 'assurapay_session=; HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=0'); return response; } catch (error) { return errorResponse(error); } }
