import {errorResponse,readiness,authorizedContextForRoute} from '../../../../../../lib/trust-app';export async function POST(r:Request,{params}:{params:{id:string}}){try{return Response.json(await readiness.paymentTriggers.activate(await authorizedContextForRoute(r),params.id))}catch(e){return errorResponse(e)}}
