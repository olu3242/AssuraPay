# Agreement Creation Security

All aggregates are workspace-scoped and the migration enables RLS. Internal comments and clause guidance are excluded from external views. Audit metadata contains hashes and references, not contract bodies. Requesters cannot approve their own requests; approval roles and assurance levels are checked per step. Signatory authority is required independently of editing access. Webhooks are authenticated before processing, replay-safe and document-hash bound.
