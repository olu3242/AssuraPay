import { trust, authorizedContextForRoute, errorResponse } from '../../../../lib/trust-app'; export async function POST(request: Request) { try { const context = await authorizedContextForRoute(request); await trust.permissions.requirePermission(context, 'party.create'); return Response.json(await trust.parties.createParty(context, await request.json()), { status: 201 }); } catch (error) { return errorResponse(error); } }
