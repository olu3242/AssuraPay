import { trust, requestContext, errorResponse } from '../../../../../lib/trust-app'; export async function POST(request: Request) { try { const context = requestContext(request); trust.permissions.requirePermission(context, 'legal.policy.administer'); return Response.json(trust.legal.createPolicy(context, await request.json()), { status: 201 }); } catch (error) { return errorResponse(error); } }
