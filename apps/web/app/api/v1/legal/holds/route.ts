import { trust, authorizedContextForRoute, errorResponse } from '../../../../../lib/trust-app'; export async function POST(request: Request) { try { const context = authorizedContextForRoute(request); trust.permissions.requirePermission(context, 'legal.hold.place'); return Response.json(trust.legal.placeHold(context, await request.json()), { status: 201 }); } catch (error) { return errorResponse(error); } }
