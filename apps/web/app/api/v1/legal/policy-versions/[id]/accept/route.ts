import { trust, authorizedContextForRoute, errorResponse } from '../../../../../../../lib/trust-app'; export async function POST(request: Request, { params }: { params: { id: string } }) { try { const context = authorizedContextForRoute(request); return Response.json(trust.legal.acceptPolicy(context, params.id, await request.json()), { status: 201 }); } catch (error) { return errorResponse(error); } }
