import{describe,expect,it}from'vitest';import{readFileSync}from'node:fs';import path from'node:path';describe('integration Batch 2 migration contract',()=>{it('defines all governance tables, RLS, append-only history and non-custodial proposals',()=>{const sql=readFileSync(path.resolve(process.cwd(),'supabase/migrations/202608030001_governance_core.sql'),'utf8');for(const table of['governed_executions','execution_history','governed_milestones','milestone_dependencies','dod_versions','dod_evaluations','certification_requests','certification_decisions','digital_certification_records','payment_trigger_definitions','payment_authorization_proposals'])expect(sql).toContain(`CREATE TABLE IF NOT EXISTS ${table}`);expect(sql).toContain('ENABLE ROW LEVEL SECURITY');expect(sql).toContain('prevent_append_only_mutation');expect(sql).not.toMatch(/transfer|debit|credit|move_funds/i);});});
