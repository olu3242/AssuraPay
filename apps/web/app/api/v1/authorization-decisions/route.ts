import {errorResponse,requestContext,treasury} from '../../../../lib/trust-app';export async function POST(r:Request){try{return Response.json(treasury.approvals.requestAuthorization(requestContext(r),await r.json()),{status:201})}catch(e){return errorResponse(e)}}
