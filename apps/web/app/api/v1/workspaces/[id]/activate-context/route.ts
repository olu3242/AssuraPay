import { trust, requestContext, errorResponse } from '../../../../../../lib/trust-app'; export async function POST(request: Request, { params }: { params: { id: string } }) { try { const current = requestContext(request); return Response.json(trust.organizations.activateContext(current.actorUserId, params.id, current.sessionId, current.identityAssuranceLevel)); } catch (error) { return errorResponse(error); } }
