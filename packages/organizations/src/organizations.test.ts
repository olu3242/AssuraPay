import { describe, expect, it } from 'vitest'; import { InMemoryTrustStore } from '@assurapay/database'; import { OrganizationService } from './index';
describe('Engine 02 multi-tenancy', () => { it('creates owner membership, validates active context, denies cross-workspace access, and blocks hierarchy cycles', () => { const store = new InMemoryTrustStore(); const service = new OrganizationService(store); const a = service.createWorkspace({ workspaceType: 'ORGANIZATION', name: 'A', slug: 'a', ownerUserId: 'owner-a', defaultCurrency: 'NGN', timezone: 'Africa/Lagos', countryCode: 'NG', correlationId: 'c1' }); const b = service.createWorkspace({ workspaceType: 'ORGANIZATION', name: 'B', slug: 'b', ownerUserId: 'owner-b', defaultCurrency: 'NGN', timezone: 'Africa/Lagos', countryCode: 'NG', correlationId: 'c2' }); expect(service.listAuthorizedWorkspaces('owner-a').map((x) => x.id)).toEqual([a.id]); expect(() => service.activateContext('owner-a', b.id, 's', 'IAL1_BASIC')).toThrow('WORKSPACE_ACCESS_DENIED'); const context = service.activateContext('owner-a', a.id, 's', 'IAL1_BASIC'); const org = service.createOrganization(context, { legalName: 'A Ltd', organizationType: 'LIMITED', industry: 'Tech', businessSize: 'SME', countryCode: 'NG', registeredAddress: 'Test address' }); const parent = service.createUnit(context, { organizationId: org.id, unitType: 'DEPARTMENT', name: 'Parent', code: 'P' }); const child = service.createUnit(context, { organizationId: org.id, parentUnitId: parent.id, unitType: 'TEAM', name: 'Child', code: 'C' }); expect(() => service.moveUnit(parent.id, child.id, context)).toThrow('ORGANIZATION_HIERARCHY_CYCLE'); }); });
