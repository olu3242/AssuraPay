import { trust, requestContext, errorResponse } from '../../../../lib/trust-app'; export async function POST(request: Request) { try { const context = requestContext(request); trust.permissions.requirePermission(context, 'party.create'); return Response.json(trust.parties.createParty(context, await request.json()), { status: 201 }); } catch (error) { return errorResponse(error); } }
