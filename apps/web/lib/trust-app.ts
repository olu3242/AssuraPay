import { InMemoryTrustStore } from '@assurapay/database'; import { IdentityService } from '@assurapay/identity'; import { OrganizationService } from '@assurapay/organizations'; import { PermissionService } from '@assurapay/permissions'; import { DeterministicVerificationProvider, PartyService } from '@assurapay/parties'; import { LegalService } from '@assurapay/legal'; import type { RequestContext } from '@assurapay/shared';
const globalTrust = globalThis as typeof globalThis & { assurapayTrustStore?: InMemoryTrustStore };
export const trustStore = globalTrust.assurapayTrustStore ??= new InMemoryTrustStore();
export const trust = { identity: new IdentityService(trustStore), organizations: new OrganizationService(trustStore), permissions: new PermissionService(trustStore), parties: new PartyService(trustStore, [new DeterministicVerificationProvider()]), legal: new LegalService(trustStore) };
export function requestContext(request: Request): RequestContext { const actorUserId = request.headers.get('x-assurapay-user-id'); const sessionId = request.headers.get('x-assurapay-session-id'); const activeWorkspaceId = request.headers.get('x-assurapay-workspace-id') ?? undefined; const tenantId = request.headers.get('x-assurapay-tenant-id') ?? undefined; if (!actorUserId || !sessionId) throw new Error('UNAUTHENTICATED'); return { actorUserId, sessionId, activeWorkspaceId, tenantId, identityAssuranceLevel: (request.headers.get('x-assurapay-assurance') as RequestContext['identityAssuranceLevel']) ?? 'IAL1_BASIC', memberships: (request.headers.get('x-assurapay-memberships') ?? '').split(',').filter(Boolean), correlationId: request.headers.get('x-correlation-id') ?? crypto.randomUUID() }; }
export function errorResponse(error: unknown) { const message = error instanceof Error ? error.message : 'UNKNOWN_ERROR'; const status = message.includes('UNAUTHENTICATED') ? 401 : message.includes('DENIED') || message.includes('REQUIRED') ? 403 : 400; return Response.json({ error: message }, { status }); }
