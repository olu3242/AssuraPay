import { describe, expect, it } from 'vitest'; import { InMemoryTrustStore } from '@assurapay/database'; import { PermissionService } from './index';
const context = { actorUserId: 'owner', sessionId: 's', identityAssuranceLevel: 'IAL2_VERIFIED' as const, activeWorkspaceId: 'w', tenantId: 't', memberships: ['w'], correlationId: 'c' };
describe('Engine 03 governed authorization', () => { it('denies by default and lets explicit denial override allowance', () => { const store = new InMemoryTrustStore(); const service = new PermissionService(store); expect(service.evaluate(context, 'party.read').allowed).toBe(false); service.grant(context, { userId: 'owner', permissionKey: 'party.read', effect: 'ALLOW', scopeType: 'GLOBAL', sourceType: 'ROLE', sourceId: 'r', effectiveFrom: '2020-01-01' }); service.grant(context, { userId: 'owner', permissionKey: 'party.read', effect: 'DENY', scopeType: 'GLOBAL', sourceType: 'PERMISSION_SET', sourceId: 'd', effectiveFrom: '2020-01-01' }); expect(service.evaluate(context, 'party.read')).toMatchObject({ allowed: false, reasons: ['EXPLICIT_DENY'] }); }); it('masks fields and blocks segregation conflicts', () => { const store = new InMemoryTrustStore(); const service = new PermissionService(store); service.createFieldPermission(context, { principalId: 'owner', objectKey: 'beneficiary', fieldKey: 'account', canView: true, canExport: false, maskingMode: 'LAST_FOUR', priority: 1 }); expect(service.serializeFields(context, 'beneficiary', { account: '12345678' })).toEqual({ account: '****5678' }); service.createSegregationRule(context, { ruleKey: 'maker-checker', firstPermission: 'pay.create', conflictingPermission: 'pay.approve', severity: 'CRITICAL', enforcementMode: 'BLOCK', status: 'ACTIVE' }); expect(() => service.assertNoSegregationConflict(context, ['pay.create', 'pay.approve'])).toThrow('SEGREGATION'); }); });
