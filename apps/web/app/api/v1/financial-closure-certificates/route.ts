import {errorResponse,authorizedContextForRoute,treasury} from '../../../../lib/trust-app';export async function POST(r:Request){try{const b=await r.json();return Response.json(await treasury.closure.issueCertificate(await authorizedContextForRoute(r),b.finalSettlementAccountId),{status:201})}catch(e){return errorResponse(e)}}
