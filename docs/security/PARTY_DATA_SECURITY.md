# Party Data Security

Raw banking and identity references are never returned by party serialization or included in audit metadata. Ordinary storage contains deterministic development tokens and masked display values. Production deployments require a managed token vault or encryption service and provider-secret management.
