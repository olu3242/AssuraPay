import { trust, errorResponse } from '../../../../../lib/trust-app'; export async function POST(request: Request) { try { const body = await request.json(); const identity = await trust.identity.register({ email: body.email, displayName: body.displayName, correlationId: request.headers.get('x-correlation-id') ?? crypto.randomUUID() }); return Response.json(identity, { status: 201 }); } catch (error) { return errorResponse(error); } }
