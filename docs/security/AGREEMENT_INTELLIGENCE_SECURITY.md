# Agreement Intelligence Security

Every record is workspace-scoped and backed by RLS contracts. Material findings and extracted items require source references. Search cannot bypass classification and returns no raw storage reference. Full documents, model payloads and privileged snippets are excluded from audit metadata. AI cannot amend, approve, publish or create binding obligations. Human reviews are separate immutable records.
