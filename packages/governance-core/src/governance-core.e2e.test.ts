import {describe,expect,it} from 'vitest';import {InMemoryTrustStore} from '@assurapay/database';import {CertificationEngine,DefinitionOfDoneEngine,ExecutionEngine,MilestoneEngine,PaymentTriggerEngine} from './index';
describe('e2e Batch 2 governance flow',()=>{it('moves execution through milestone, DoD, human certification and non-custodial payment proposal',()=>{const store=new InMemoryTrustStore();const ctx={actorUserId:'owner',sessionId:'s',identityAssuranceLevel:'IAL2_VERIFIED' as const,activeWorkspaceId:'w',tenantId:'t',memberships:['w'],correlationId:'e2e'};const executionEngine=new ExecutionEngine(store);const execution=executionEngine.create(ctx,{contractId:'contract',title:'Execute',ownerUserId:'owner'});executionEngine.transition(ctx,execution.id,'PLANNED','plan');executionEngine.transition(ctx,execution.id,'ACTIVE','start');const milestone=new MilestoneEngine(store).create(ctx,{executionId:execution.id,title:'Deliver',ownerUserId:'owner',durationDays:5});const dodEngine=new DefinitionOfDoneEngine(store);const definition=dodEngine.publish(ctx,dodEngine.createVersion(ctx,milestone.id,[{key:'evidence',description:'Evidence accepted',mandatory:true,evidenceRequirementKeys:['proof'],evaluationType:'MANUAL'}]).id);const evaluation=dodEngine.evaluate(ctx,definition.id,{facts:{},evidence:{proof:'sha256:proof'},manualResults:{evidence:true}});const certEngine=new CertificationEngine(store);const request=certEngine.request(ctx,{executionId:execution.id,milestoneId:milestone.id,dodEvaluationId:evaluation.id,reviewerIds:['reviewer']});const reviewer={...ctx,actorUserId:'reviewer'};certEngine.decide(reviewer,request.id,'APPROVE','verified',['sha256:proof']);const certificate=certEngine.issue(reviewer,request.id);const payment=new PaymentTriggerEngine(store);const trigger=payment.define(ctx,{milestoneId:milestone.id,name:'Pay on certification',requiredDodDefinitionId:definition.id,certificationRequired:true,amountMinor:250000,currency:'NGN'});expect(payment.propose(ctx,trigger.id,'proposal-1')).toMatchObject({status:'PROPOSED',certificationId:certificate.id,amountMinor:250000});expect(store.list('auditRecords').length).toBeGreaterThan(2);expect(store.list('outboxEvents').length).toBeGreaterThan(2);});});
