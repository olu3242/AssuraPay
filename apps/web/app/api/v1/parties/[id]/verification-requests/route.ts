import { trust, authorizedContextForRoute, errorResponse } from '../../../../../../lib/trust-app'; export async function POST(request: Request, { params }: { params: { id: string } }) { try { const context = await authorizedContextForRoute(request); await trust.permissions.requirePermission(context, 'party.verify', params.id); const body = await request.json(); return Response.json(await trust.parties.requestVerification(context, { ...body, partyId: params.id }), { status: 201 }); } catch (error) { return errorResponse(error); } }
