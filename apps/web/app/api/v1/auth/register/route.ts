import { trust, enterIdentityBoundaryScope, errorResponse } from '../../../../../lib/trust-app'; export async function POST(request: Request) { try { enterIdentityBoundaryScope(); const body = await request.json(); const correlationId = request.headers.get('x-correlation-id') ?? crypto.randomUUID(); const identity = await trust.identity.register({ email: body.email, displayName: body.displayName, correlationId }); const result = process.env.ASSURAPAY_DEPLOYMENT === 'test' ? await trust.identity.activate(identity.id, correlationId) : identity; return Response.json(result, { status: 201 }); } catch (error) { return errorResponse(error); } }
