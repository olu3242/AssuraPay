import {readFileSync} from 'node:fs';import {resolve} from 'node:path';import {describe,expect,it} from 'vitest';describe('integration Batch 4 agreement intelligence migration',()=>{it('defines all engine tables, RLS, immutable records and source grounding',()=>{const sql=readFileSync(resolve('supabase/migrations/202608030003_agreement_intelligence.sql'),'utf8');for(const t of ['contract_analysis_runs','contract_risk_assessments','contract_versions_v2','contract_repository_documents','agreement_intelligence_versions'])expect(sql).toContain(`TABLE IF NOT EXISTS ${t}`);expect(sql).toContain('source_references');expect(sql).toContain('ENABLE ROW LEVEL SECURITY');expect(sql).toContain('prevent_append_only_mutation')})});
