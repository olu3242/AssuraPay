# Agreement Intake Risk Register

HIGH — RLS migration must match production DB session-context naming; verify live before merge.
HIGH — route handlers not yet implemented, so auth/permission behavior is not certified.
HIGH — UI not yet wired, so user-visible three-path flow is not browser-certified.
MODERATE — migration registry/convention may require additional repository-specific wiring.
MODERATE — convergence service uses deterministic intake contract number; confirm existing uniqueness/idempotency semantics under concurrent conversion.
LOW — clarity required-term set is intentionally minimal for first slice and should become profile-aware without weakening mandatory commercial terms.

No risk is closed by documentation alone; close with executable evidence.
